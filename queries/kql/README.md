# KQL Queries

Ce dossier contient les requêtes KQL utilisées par ADF / Log Analytics.

- `adf_activity_run_rowcount_main.kql` : requête principale pour récupérer `row_count_adf_ingestion_copie_parquet`, `adf_start_ts`, `adf_end_ts`, `adf_duration_sec`.
