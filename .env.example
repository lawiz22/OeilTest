# Azure SQL auth for Oeil
# Option A (recommended): set only the password and keep host/db/user defaults from code
OEIL_AZURE_SQL_PASSWORD=YOUR_PASSWORD_HERE

# Option B (advanced): override full connection string
# If set, this takes precedence over OEIL_AZURE_SQL_PASSWORD
# OEIL_AZURE_SQL_CONN_STR=Driver={ODBC Driver 18 for SQL Server};Server=tcp:testbanque.database.windows.net,1433;Database=testbanque;Uid=oeil_ctrl_login;Pwd=YOUR_PASSWORD_HERE;Encrypt=yes;TrustServerCertificate=no;
